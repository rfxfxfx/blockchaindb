# Netlify build config for the Next.js app.
[build]
  command = "npm run build"
  publish = ".next"

# Runs the app (SSR + API routes) on Netlify Functions.
[[plugins]]
  package = "@netlify/plugin-nextjs"
